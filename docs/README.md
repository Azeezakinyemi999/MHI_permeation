:::{warning}
This page is `README.md` from the repository root, and it is **out of date**. It
names Incoloy 802 as the active system, but
`calculations/config/model_config.py` sets
`ACTIVE_STUDY = 'Guo_etal_2025_316L'`; and its repository-structure listing
includes a `data/` package and `config/model_config1.py` that no longer exist.

{doc}`PARAMETERS` and {doc}`OUTPUTS` are generated against the real active study
and are authoritative where they disagree with this page.
:::

```{include} ../README.md
```
