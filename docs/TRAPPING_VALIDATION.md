:::{warning}
Appendix C of this page links to `data/material_data.py`,
`data/oxide_properties.py` and `data/sensitivity_parameters.csv`. That `data/`
package was removed — material data now resolves through
{doc}`api/config`. The validation argument itself is unaffected.
:::

```{include} ../TRAPPING_VALIDATION.md
```
